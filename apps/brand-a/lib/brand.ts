import type { BrandConfig } from "@repo/ui";

export const brand: BrandConfig = {
  slug: "brand-a",
  name: "Brand A",
  tagline: "Placeholder brand — rename in lib/brand.ts and app/globals.css once real names are locked in.",
};
